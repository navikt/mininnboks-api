package no.nav.sbl.dialogarena.mininnboks.common.audit

enum class AuditIdentifier {
    FAIL_REASON,
    DENY_REASON,
    TYPE,
    JOURNALPOST,
    DOKUMENT,
    BEHANDLINGSKJEDEID,
    BEHANDLINGSID
}
